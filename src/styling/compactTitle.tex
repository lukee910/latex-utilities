% Display title

% Title itself
\setlength{\droptitle}{-3\baselineskip} % Move the title up
\pretitle{\Large\bfseries} % Article title formatting
\posttitle{\medskip \\}

% Author and date
% \preauthor{\begin{center}\normalsize}
% \postauthor{\end{center}\normalsize}
% \predate{\begin{center}\normalsize}
% \postdate{\end{center}\normalsize}
\preauthor{\normalsize}
\postauthor{,}
\predate{\normalsize}
\postdate{.}
