% === Typesetting, text

% Properly support more than ASCII characters
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Fonts for text and math
% (loads amsmath and textcomp)
\usepackage{kpfonts}

% Improve justification etc.
\usepackage{microtype}

% === Layout, formatting, titles

% Lists
\usepackage{enumitem}

% Table formatting
\usepackage{booktabs}

% Heading formatting
\usepackage{titlesec}

% Headers and footers
\usepackage{fancyhdr}

% Main title formatting
\usepackage{titling}

% === Others

% Clickable references
\usepackage[colorlinks=true]{hyperref}

% Named references
\usepackage{nameref}
