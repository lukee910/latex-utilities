% === Document class
\documentclass[a4paper, twocolumn, 10pt]{article}

% === Packages
\usepackage[ngerman]{babel}
\usepackage[margin=1.75cm, bottom=2.25cm]{geometry}

% === Utilites from this repo
\newcommand{\utilpath}{\currfiledir../..}

\RequirePackage{\utilpath/all}

% === Commands, definitions, etc.

% === Template styling
\RequirePackage{\utilpath/styling/basicTitle}
\RequirePackage{\currfiledir styling}
