% === Document class
\documentclass[10pt,landscape,a4paper]{article}

% === Packages
\usepackage[ngerman]{babel}
\usepackage[margin=0.5cm]{geometry}

% Multi col layout
\usepackage{multicol}

% === Utilites from this repo
\input{\lutilpath/all}

% === Commands, definitions, etc.

% Textsize
% \footnotesize

% === Template styling
\input{\lutilpath/templates/cheatsheet/styling}
