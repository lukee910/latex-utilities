% === Document class
\documentclass[10pt, a4paper, twocolumn]{article}

% === Packages
\usepackage[english]{babel}

% Document margins
\usepackage[hmarginratio=1:1,top=32mm,columnsep=20pt]{geometry}
\usepackage[hang, small,labelfont=bf,up,textfont=it,up]{caption}

% Headers and footers
\usepackage{fancyhdr} 

\usepackage{titling}

% for correct hyphenation and T1 encoding
\usepackage[T1]{fontenc}						
\RequirePackage{lmodern}
% Helvetica
\RequirePackage{tgheros}
% for character protrusion and font expansion
\RequirePackage{microtype}					

\usepackage{xcolor}	

% === Utilites from this repo
\input{\lutilpath/all}

% === Commands, definitions, etc.

\renewcommand{\familydefault}{\sfdefault}

% All pages have headers and footers
\pagestyle{fancy} 

% Include ETH logo in header
\lhead{\includegraphics[height=4mm]{\lutilpath/elements/eth_logo_kurz_pos}}

% Display title
\setlength{\droptitle}{-4\baselineskip} % Move the title up
\pretitle{\begin{center}\Huge\bfseries} % Article title formatting
\posttitle{\end{center}}

% === Template styling

